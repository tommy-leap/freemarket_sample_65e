-breadcrumb :show
=render 'layouts/breadcrumbs'
%main.identification
  =render 'shared/side_bar'
  .card-content
    %h2.card-content__head 支払い方法
    .card-content__list
      .card-content__list__show
        .card-content__list__show__text
          クレジットカード一覧
      .card-content__list__payment
        %form.card-content__list__payment__form
        .card-content__list__payment__brand
          .card-content__list__payment__brand__picture
          = image_tag @card_src, width: '26', height: '20', alt: @card_brand, id: "card_image"
        .card-content__list__payment__number
          = "**** **** **** " + @default_card_information.last4
        .card-content__list__payment__date
          - exp_month = @default_card_information.exp_month.to_s
          - exp_year = @default_card_information.exp_year.to_s.slice(2,3)
          = exp_month + " / " + exp_year
        = form_tag(delete_card_index_path, method: :post, id: 'charge-form',  name: "inputForm") do
          %input{ type: "hidden", name: "card_id", value: "" }
          %button.delete-button 削除する
      .card-content__list__regist
        支払い方法について >  